{
  "name": "koala",
  "version": "0.0.1",
  "description": "Southwest Koala monorepo",
  "homepage": "https://github.com/southwestkoala/koala/README.md",
  "private": true,
  "scripts": {
    "dev": "pnpm -r dev --filter {apps}",
    "build": "pnpm package && pnpm build --filter {apps}",
    "package": "pnpm package --filter {packages}",
    "lint": "pnpm -r lint --filter {apps} --filter {packages}",
    "format": "pnpm -r format --filter {apps} --filter {packages}"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^5.9.0",
    "@typescript-eslint/parser": "^5.9.0",
    "eslint": "^8.6.0",
    "eslint-config-prettier": "^8.3.0",
    "prettier": "^2.5.1",
    "tslib": "^2.3.1",
    "typescript": "^4.5.4"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/southwestkoala/koala.git"
  }
}
